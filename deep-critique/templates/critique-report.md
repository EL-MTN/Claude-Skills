# Critique report template

Fill this in. Cut any section that would be empty rather than padding it. Blockers and majors come first; never let polish bury them.

---

## Design critiqued

- **Artifact:** <image path / URL / Figma node>
- **Viewport / device judged:** <e.g. desktop 1440px, mobile 390px>
- **Context:** <audience / brand / platform / stated concern from $2, or "none given">
- **Lenses run:** <list>

## What's working

2–4 specific strengths — so the user knows what not to touch.

- **<strength>** — <why it's good, specifically>.

## Findings (prioritized)

Ranked by severity, then confidence. Merge cross-lens overlaps into one entry and note the convergence.

### 1. <one-line issue> — `<blocker|major|minor|polish>`

- **Where:** <pointable location>
- **Lens(es):** <lens, or "usability + a11y (converged)">
- **Why it matters:** <named principle / standard> — <the observed evidence>
- **Fix:** <concrete change, or "—" if leaving it to the designer>

### 2. <one-line issue> — `<severity>`

- **Where:** …
- **Lens(es):** …
- **Why it matters:** …
- **Fix:** …

<continue, in priority order>

## Dropped in verification (optional)

If you cut notable findings as taste-only or unverifiable, list them in one line each so the user sees the critique was filtered, not just short.

- <claim> — dropped: <preference, not a defensible principle / couldn't locate in artifact>.

## Top 3, if you do nothing else

The three highest-leverage fixes, in order.

1. <fix>
2. <fix>
3. <fix>
